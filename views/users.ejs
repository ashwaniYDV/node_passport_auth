<div class="container"></div>
    <h1 class="text-center text-success">Users:</h1>
    <% users.forEach((user)=>{ %>
        <p><%= user.name %></p>
    <% }) %>
    <a href="/dashboard" class="btn btn-primary">Dashboard</a>
</div>